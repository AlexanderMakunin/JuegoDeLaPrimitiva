name: Java CI with Gradle

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      # 📌 Descarga el código del repositorio
      - name: Checkout repository
        uses: actions/checkout@v4

      # 📌 Configura JDK 17
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 📌 Configura Gradle y la caché automática
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4 

      #Dar permisos de ejecución al script gradlew 
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # 📌 Ejecuta la compilación del proyecto con Gradle Wrapper
      - name: Build with Gradle Wrapper
        run: ./gradlew build --no-daemon # --no-daemon evita problemas en GitHub Actions

  dependency-submission:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

     
