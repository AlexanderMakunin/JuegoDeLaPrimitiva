name: Java CI with Gradle

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run Checkstyle and save output
        run: ./gradlew checkstyleMain > checkstyle-output.txt

      - name: Comment Checkstyle output on PR
        id: checkstyle-comment
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          path: checkstyle-output.txt
          header: Checkstyle Report

      - name: Build with Gradle
        run: ./gradlew clean build --no-daemon
        continue-on-error: false  # Fail the job if build fails
       # if: steps.checkstyle-comment.outcome == 'success' # Require checkstyle pass before build (optional)
